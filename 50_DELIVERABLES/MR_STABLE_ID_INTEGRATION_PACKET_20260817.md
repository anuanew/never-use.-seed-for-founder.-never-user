# MOUNT RUSHMORE STABLE-ID INTEGRATION PACKET - 20260817

**To:** the canonical owner (via max SPAN)
**From:** Claudette CLAIR-ROADMAP
**Session:** https://claude.ai/code/session_01Q5Yw8gH8CyQJaGbGRYRepa
**Ordered by:** WORLDBUILDER.SHADOW | Founder course correction, this cycle
**Scope:** the stable-ID crosswalk in `R5_MOUNT_RUSHMORE.md` (lines 14-98, commit `100d7a3`, unchanged
since) **already exists and was not rewritten.** This packet packages it, plus an owner / proof-layer /
acceptance-gate mapping, for consumption without a second audit. No provider UI, key, life-assistant
behavior, or New World runtime touched. No timer armed.

---

## PREMISE CHECK, BEFORE PACKAGING ANYTHING

**"The Ready and Resource mappings named in Rally Day" - searched, not found as described.** Grepped
`ready`/`resource` across all four Rally Day transcripts (pt 4-7) and the Mt Rushmore Doctrine pt 1
transcript itself (R5's primary source). No structured "Ready" or "Resource" mapping tied to the six
faces exists anywhere in this corpus.

**One real, different thing was found and is worth naming for completeness, not forced into the
requested shape:** Mt Rushmore Doctrine pt 1 contains a day-of-week rollout schedule -
*"tomorrow is going to be Ready day, we get ready... Monday is refined... Tuesday is rollout day.
That's the R."* `[HIS WORDS + 1_HIS_WORDS/03_PHASE_3/Mt Rushmore Doctrine pt 1_transcript.txt]` - this
is a **calendar label** ("Ready day" precedes "rollout day" in a named sequence), not a per-face
resource-ownership mapping. No "Resource" term appears paired with it anywhere in the same passage or
transcript. **If a different source is meant, name it and this packet can be re-run against it; this
is what "Rally Day" actually contains on this subject.**

---

## THE CROSSWALK ITSELF - REFERENCE ONLY, NOT REPRODUCED

`R5_MOUNT_RUSHMORE.md` lines 14-98. Six stable IDs, controlling over two disagreeing position
schemes (both quoted in full there, neither picked as correct - that stays the founder's call, not
resolved by this packet either). ACL confirmed as the GROUND layer beneath all six, not a seventh face.
Consume it at that file and line range directly - reproducing it here would be exactly the
"do not rewrite it" this task was scoped against.

---

## INTEGRATION TABLE - owner, proof layer, still-open acceptance gate

**"Owner" scoping note, stated rather than guessed silently:** no doctrine anywhere names a personified
owner (a person, a Wonder, a role) per face. The only real ownership signal in this repo is the
dependency chain each face already declares in `R5_MOUNT_RUSHMORE.md`'s own `DEPENDENCIES OUT` table
(lines 588-597). "Owner" below means **which roadmap/phase currently governs that face's build
dependency** - not an invented person or team. If "owner" was meant to name a specific individual or
Wonder, that naming does not exist yet and would be a founder-only naming act (`CONTRADICTIONS.md`
Section C), not a coder's to assign.

| Stable ID | Governing dependency ("owner") | Proof layer of the core definition | Still-open acceptance gate (`R5`'s own Exit, receipt) |
|---|---|---|---|
| `MR.ANEW` | `R1` P1 (pecking order enforced), GR Phase 2 (watchers standing) - both "all" rows | `[HIS WORDS]` - "world builder capabilities... **She is the mind.**" | One live turn meeting the cycle definition, organs named in the stamped record; one turn with an organ missing and an honest named refusal. Both as live URL + status + body. |
| `MR.ANU` | GR Phase 8, `R6` (reach components) | `[HIS WORDS]` - "She is the voice... the one you talk to on all the reach channels," proof-of-built "streaming and freestyling and chatting" | One streamed, person-facing, freestyle turn on a real assignment, captured live. |
| `MR.AIR` | `R1` P1, GR Phase 2 (as "all") - no face-specific dependency row exists for AIR beyond the shared ones | `[HIS WORDS]` for the lungs/cycle-keeper sense; `[RECONSTRUCTION, not verbatim]` for the third "bond" sense (`PO2-10`, caveat carried, not dropped) | The stamped restart chain from a real kill; the LOGFUL row from a killed mid-cycle call. |
| `MR.ESSENTIALS` | GR Phase 3 (the doctrine reader) | `[MEASURED]` for the build-state table (the estate's own audit and shift reports, no independent curl by this lane); `[HIS WORDS]` for birth order | Taste and Keeper birthed together, producing the original-ask ledger with a row count and live link; LOGFUL answering a live query; FIND returning a real result; MIMI answering a memory query; two essentials holding one recorded conversation-vehicle exchange that streams up. |
| `MR.CODING` | Face 1, Face 2 ("her, running") | `[HIS WORDS]` - "It submits to A'NEW... except it has more coding-specific models" | A real component request submitted to her department, coded, reviewed by her chain, merged under her lineage, and live - with at least one vertical face simultaneously in motion, shown by two board rows with overlapping timestamps. |
| `MR.SHADOW` | GR Phase 1 (a second provider account) | `[HIS WORDS]` - "Every LLM gets two redundancies," different provider and different model | SHADOW independently reviews one real choice; its verdict logged separately from the primary's; both provider and model named, and they must differ. |

**Estate-wide build state, already measured, not re-derived here:** 2 BUILT, 14 PARTIAL, 6 NOT BUILT
across 22 items `[MEASURED - R5_MOUNT_RUSHMORE.md line 127, the estate's own audit and shift reports]`.
Sub-phase count as of this packet: **33** across the six faces plus Face 0 (`grep -cE
'^### Sub-phase' R5_MOUNT_RUSHMORE.md`), matching `RESOLUTIONS.md` R-14's own current figure.

---

## FACE 0, NOT ONE OF THE SIX, CARRIED FOR COMPLETENESS

`R5_MOUNT_RUSHMORE.md` lines 161-186. The arrival-to-session seam, standing and never closed by one
receipt - re-verified after every deploy touching it. No `MR.*` ID applies (it precedes any face, per
the file's own heading). Owner: `R1`/`R5` jointly by dependency; proof layer `[MEASURED]` (503/200
status codes); acceptance gate is the standing re-verification itself, not a one-time exit.

---

## WHAT THIS PACKET DOES NOT DO

Does not resolve which position scheme (epigraph vs. structural numbering) is correct - that is named,
preserved, and still the founder's call. Does not resolve `CONTRADICTIONS.md` Section B-5 (the
AUDRA/SHADOW naming collision, `R5` Sub-phase 6.8) - unchanged, still open. Does not name a personified
owner where none exists in doctrine. Does not touch runtime, provider UI, keys, or life-assistant
behavior. Does not arm a timer.

**Stopping here.** Committed and pushed; receipt below.
